<!DOCTYPE html>
<html>
<head>
  <title>Portfolio</title>
  <%= javascript_include_tag "application" %>
  <%if @custom_stylesheet%>
  	<%= stylesheet_link_tag    "chess" %>
  <%else%>
  	<%= stylesheet_link_tag    "application", :media => "all" %>
  <% end %>
  <%= csrf_meta_tags %>
  <meta charset="UTF-8">
</head>
<body>
 <div class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <a href="/about/" class="navbar-brand">John Wahba</a>
          <button class="navbar-toggle" type="button" data-toggle="collapse" data-target="#navbar-main">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
        </div>
        <div class="navbar-collapse collapse" id="navbar-main">
          <ul class="nav navbar-nav">
            <li>
              <a href="/about/">About</a>
            </li>
            <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#" id="Projects">Projects <span class="caret"></span></a>
              <ul class="dropdown-menu" aria-labelledby="download">
                <% Project.all.each do |project| %>
                  <li><a tabindex="-1" href="<%= project_url(project.slug) %>"><%= project.title %></a></li>
                <% end %>
                <li class="divider"></li>
                <li><a tabindex="-1" href="<%= projects_url %>">Index</a></li>

              </ul>
            </li>
          </ul>
        </div>
      </div>
    </div>
  <div class='container fill'>

  	<div class='page-header'> 
  		<h1 class='text-center'> John Wahba </h1>
  	</div>

  	<%= yield %>

  </div>

</body>
</html>
